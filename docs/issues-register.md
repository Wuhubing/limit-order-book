# Issue Register — Baseline (Hermes static review, 2026-09-07)

Upstream: `brprojects/Limit-Order-Book`, commit `af6e5349874649fe196bd6c26653d357f5a751f2` (2024-06-12).
Status legend: `READING` = confirmed by code inspection, needs runtime repro; `HYPOTHESIS` = plausible, needs test; `CONFIRMED` = runtime evidence exists (filled by later tasks); `FIXED` / `OUT_OF_SCOPE` as decided.
Scope note: portfolio research scope = limit/market/cancel/modify + FIFO price-time core. Stop & stop-limit code shares data structures and must not regress (existing tests cover it) but is not primary research scope.

| # | Area | Item (file:line evidence) | Type | Status |
|---|------|---------------------------|------|--------|
| 1 | Ownership | Duplicate order id: `orderMap.emplace` (Book.cpp:96) silently fails but the new `Order` is still appended to the limit queue; two live orders share an id, map points to the first. Cancel/execute of the id then deletes the wrong object; the unmapped order leaks and can be reached later through the limit list (UAF). No rejection path exists. | correctness/ownership | READING |
| 2 | Edge pointer | `updateBookEdgeRemove` (Book.cpp:638) picks `leftChild`/`rightChild`/`parent` heuristically when the best level empties. Under strict AVL shape this is correct (max node's left subtree has height<=1), but `Limit::~Limit` (Limit.cpp:10) splices BST nodes WITHOUT rebalancing, and `deleteLimit` (Book.cpp:742) only re-balances ancestors of the deleted node — not the in-order-successor splice path inside the subtree. AVL invariants can degrade => edge pointer can point at a non-extreme level => missed/incorrect matches. Needs crafted-input repro. | correctness | HYPOTHESIS |
| 3 | Semantics | `modifyLimitOrder` (Book.cpp:132) = cancel + passive re-place. If the new limit crosses the book (buy modify to price >= lowestSell), the order rests crossed: invariant "no immediately-crossable resting orders" is violated (a freshly ADDED order would have executed, addLimitOrder:91). Buy book can show bestBid >= bestAsk. | correctness/invariant | READING |
| 4 | Semantics | Market order with insufficient/empty opposite side (marketOrderHelper:1002) simply vanishes — no rejection event, no partial state, order id never tracked. Defined-ish ("forgotten") but must be documented & mirrored exactly by the reference model. | semantics | READING |
| 5 | I/O in engine | `searchOrderMap` (Book.cpp:308) / `searchLimitMaps` / `searchStopMap` print to stdout on EVERY miss. Cancel/modify of unknown ids => per-op stdout write (syscall in hot path). Tests call these on misses routinely (test output noise). Engine should be print-free; reporting via optional hook/return. | performance/design | READING |
| 6 | Destructor | `Book::~Book` (Book.cpp:13) deletes Orders via orderMap, then Limits via unordered_map iteration order. `~Limit` does BST pointer surgery touching `parent`/children; if a child was already deleted (map order arbitrary), the parent destructor dereferences freed nodes => UB/ASan crash risk on Book teardown with leftover levels. | ownership | HYPOTHESIS |
| 7 | Ownership | `existingOrderAsMarketOrder` (Book.cpp:864): triggered stop-limit converts by `delete headOrder` BEFORE matching; if remainder then rests it reuses the freed-then-? (no: delete happens only in the fully-filled branch — check partial branch: setShares+append reuse). Verify no double-free when remainder path also runs deleteFromOrderMap later. | ownership | HYPOTHESIS |
| 8 | Perf | AVL balance factors are NOT stored; every `balance()` recomputes subtree heights by full recursion (`getLimitHeight` Book.cpp:288 calls both subtrees recursively). Each insert/delete rebalance walks O(subtree) — with many price levels this dominates. Also destructor-splice deletions can degrade tree shape. | performance | READING |
| 9 | Map keying | `stopMap` keyed by price only, shared by buy and sell stop levels (Book.hpp:27, addStop:541). Two-sided stop at same price: second `emplace` fails, level still inserted into its tree; cancel of one side erases the other side's map entry (deleteFromStopMap Book.cpp:807) => bookkeeping divergence. Out of primary scope; must not regress; repro optional. | correctness | HYPOTHESIS |
| 10 | Validation | No input validation anywhere: shares<=0, price<=0, negative id accepted (Order.hpp int fields). `partialFill` can drive shares negative on inconsistent inputs; negative volume corrupts level totals. "Explicit invalid-input handling" required => reject path in fixed engine. | correctness | READING |
| 11 | Overflow | `int` shares/volumes; `totalVolume` sums across orders in a level (Limit.hpp int). Large books can overflow silently. Benchmark uses moderate sizes; bounds documented, generator clamps. | robustness | READING |
| 12 | Ordering | `executedOrdersCount`/`AVLTreeBalanceCount` are instance fields reset at each public op (Book.cpp:79-80) — pipeline stats work only because single-threaded sequential. Trade events are not emitted anywhere (only counts): structured trade records must be added for the research core. | design | READING |
| 13 | Semantics | Modify at the SAME price also re-appends at the tail (cancel+append) — order loses its FIFO position even for pure size changes. Document as defined behavior (modify = cancel-and-replace), reference model must match; do not silently "fix" to priority-preserving. | semantics | READING |
| 14 | Build | Root CMakeLists requires cmake>=3.29 and `add_subdirectory(googletest)` with googletest absent from the repo (gitignored). Build is broken out-of-the-box; needs FetchContent bootstrap. test/ExampleOrdersTests.cpp references Windows absolute paths (lines 33,37) and ./Orders.txt / initialOrders.txt do not exist in repo — environment-stubbed tests. | build | CONFIRMED |

## Resolution log (2026-09-07, after LOB-002 merge — commit 1bd1b2e)
- #1 duplicate-id reject: FIXED. addLimitOrder rejects ids already live (orderMap covers limit+stop orders), zero state change; reuse allowed after death. Repro tests: EngineCorrectnessTests.DuplicateIdAddIsRejected / DuplicateIdReusableAfterCancel. Two upstream tests that accidentally relied on dup-id acceptance were adapted with unique ids (commit 9e9c827, intent preserved).
- #2 AVL delete: FIXED. ~Limit inert; deleteLimit/deleteStopLevel implement classic AVL delete (successor splice, rebalance from successor's original parent AND — review catch, commit d5e8061 — from the successor itself when it is the deleted node's direct right child). Edge heuristics still assume AVL shape, now sound again.
- #3 modify-to-crossing: FIXED. modifyLimitOrder executes aggressively like AddLimit; remainder rests at new limit; full fill deletes the order (id reusable). I5 (no-cross) holds. Repro tests: ModifyBuyToCrossingExecutesFullFill / ModifyToCrossingLeavesRemainderResting / SamePriceModifyLosesFifoPriority (kept upstream cancel-and-replace FIFO semantics, #13 unchanged by design).
- #4 market remainder drop: KEPT as upstream semantics, now documented in docs/semantics.md S3 and pinned by MarketOrderRemainderDropped test.
- #5 prints: FIXED. search* return nullptr silently (commit c91e90d); engine is print-free.
- #6 teardown: FIXED. Book::~Book deletes orders via orderMap, levels post-order per tree (deleteTree); no destructor surgery. Local verification UBSan-clean; ASan/LSan deferred to Linux CI (host limitation: Apple clang 16 + macOS 26 ASan init broken, probe-verified).
- #8 balance-factors-not-cached: NOT fixed (by design) — true O(subtree) rebalance cost; retained as the target for the Stage D price-structure optimization experiment (LOB-006), with upstream tests pinning behavior.
- #9 stopMap two-sided keying: KEPT (out of research scope); covered by upstream stop tests; noted in docs.
- #10 validation: FIXED per semantics.md (id/qty/price <= 0, dup live id => silent reject). Tests: ValidationRejectsLeaveStateUntouched.
- #11 overflow bounds: DOCUMENTED (semantics.md corpus bounds; per-level volume < 2^31-1).
- #12 events/state introspection: ADDED — FillEvent sink + snapshot()/LevelState/OrderState + Order::getNextOrder (commit d5e8061), covered by FillEventsCorrect / SnapshotMatchesState / NullFillSinkIsSafe.
- #14 build: FIXED in LOB-001 (googletest FetchContent bootstrap).
Evidence: LimitOrderBookTests 123/123 (Release + UBSan), EngineCorrectnessTests 18/18 (Release + UBSan), verify.py 8/8.
- LOB-003 (merged 9571fa8): independent reference model (reference/, std-map/deque oracle, no engine headers) + DifferentialHarness comparing FILL STREAMS and full book state after EVERY request + I1-I7 invariant checker + greedy minimizer + 20 seeds x 2000 requests fuzz + 3 committed corpora + tools/diff_fuzz replay CLI (exit 0/1/2). Result: ZERO divergence between engine v1 and reference across all differential runs; DeterminismTests/DifferentialTests 19/19 Release + UBSan; no failing seed exists to minimize. Engine correctness is now pinned by independent oracle, not just self-tests.

## Baseline rules (decided, control plane)
- Correctness fixes land ONLY with a reproducing test committed first (separate commits).
- Reference model semantics = the FIXED engine semantics, defined in docs/semantics.md before differential tests.
- Upstream original performance numbers, if reproduced, are recorded separately from the fixed-baseline numbers.
- Benchmark correctness checks, sanitizers, and timing use different build configs.
